ML-DS practice projects:

first one : House price prediction using Linear Regression. 
            few data science libraries used are:
            --NumPy
            --Pandas
            --Matplotlib
            --Scikit-learn